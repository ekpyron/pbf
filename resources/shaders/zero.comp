#version 460
#extension GL_ARB_separate_shader_objects : enable

layout (local_size_x_id = 0, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0, set = 1) writeonly buffer Data
{
    vec4 data[];
};

void main (void)
{
    data[gl_GlobalInvocationID.x] = vec4(0,0,0,0);
}
